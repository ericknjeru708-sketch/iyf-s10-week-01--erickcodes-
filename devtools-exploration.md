# DevTools Exploration Report - Week 1

## Website 1: https://example.com
1. HTML tags used: `<!DOCTYPE html>`, `<html>`, `<head>`, `<title>`, `<body>`, `<h1>`, `<p>`, `<div>`, `<a>`
2. Page title: Example Domain
3. Number of headings: 1 (`<h1>`)

## Website 2: https://developer.mozilla.org
1. Navigation menu is wrapped in the `<nav>` tag
2. Search bar is inside a `<form>` element with `<input type="search">`
3. On hover, links change color and get an underline (CSS `:hover` effect)

## Website 3: https://github.com
1. Five different HTML elements: `<header>`, `<nav>`, `<main>`, `<form>`, `<button>`
2. Form element: The search bar at the top (`<form>` with text input)
3. Screenshot: Taken of the Elements panel showing the page structure

**What I Learned:**
- How to use Chrome DevTools to inspect real websites
- Importance of semantic HTML tags
- Understanding how CSS styles are applied
